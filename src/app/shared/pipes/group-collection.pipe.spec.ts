import { GroupCollectionPipe } from './group-collection.pipe';

describe('GroupCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
